<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="com.kerbysoft.qwikcut.speakstats.GameFeedActivity">
    <!-- "Mark Favorite", should appear as action button if possible -->
    <item
        android:id="@+id/exportButton"
        android:icon="@drawable/ic_action_file_file_upload"
        android:title="Export"
        app:showAsAction="ifRoom"/>

    <!-- Settings, should always be in the overflow -->
    <item android:id="@+id/saveGameButton"
        android:icon="@drawable/ic_action_content_save"
        android:title="Save Game"
        app:showAsAction="ifRoom"/>

</menu>
